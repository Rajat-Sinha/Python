#OS

import os
os.file.exists(path) #used for checling whether the dirctory path exists or no
os.makedirs(path) #used for making directory path
os.path.join(directory_name,file_name) it inserts the file_name inside th directory_name directory_name\file_name
os.path.isfile(file_name) #returns true if file is an existing regular file
os.getcwd() #Return a string representing the current working directory.
os.chdir(path) #Change the current working directory to path.