#Modules
#Modules is the basically to use the function in anther program and include the modules file

import Module # import keyword is used to add anther file and to use it
import random # this is basically random.py

Module.name() # this basically used to call the function form another file
x = random.randrange(1,100) # randrange is the function built inside random file and used for getting random value between 1 and 100
print(x)

